# Claude handoff: standalone Fusion remote agents

Updated 2026-09-19 06:30 UTC. This handoff covers only the operator's narrow scope: cross-server Codex/Claude visibility, direct feedback to the exact native agent, and per-session token counts, unit rates and estimated costs. AgentPulse is not a dependency and must remain untouched unless the operator separately asks to retire it.

## Current outcome

The product implementation is complete and live on J. Open `http://wj:4040/?project=proj_1522a34f98694e79`, select **Fusion Canonical Source**, then **More views → Agents → Remote agents**. The live browser shows J and m3 connected and m5 offline. Opening a session shows bounded native activity, fresh/cached/cache-write/output/reasoning tokens, per-token and per-million USD rates, pricing provenance and an estimated cost. Feedback is enabled only when that exact session generation has a current native hook capability; historical or unsupported sessions stay disabled.

The active J artifact is `/home/ubuntu/fusion-deployments/artifact-1a595c65c7`. Its clean source is `/home/ubuntu/fusion-deployments/remote-agents-deploy-20260919/source` at local deployment commit `1a595c65c732a7ea441cda7333fd577079453f54`, tree `71e090f24169e99ef6f716c5a8d9fda92bb0b51b`. It is based on canonical `8a83f875bae673e3692bab0da36cc93851801f24`, the accepted standalone tree at deployment commit `02ab7f4f256ae6cc95cc254472dee3bcff8d7bd7`, plus the WireGuard HTTP `crypto.randomUUID` fallback from owned commit `e016031be`. `fusion-daemon.service` and `fusion-remote-agents-collector.service` are active; health, database and task identity are healthy.

Full current-main acceptance is GitHub Actions run `35395556505`, validation SHA `924229521171f0b75d2710c610bca3090f779658`, SUCCESS. It covers PostgreSQL migrations 0084/0085, project isolation/auth, ingestion/replay, bounded reads, pricing, feedback queue/claim/ack fencing, restart/outage behavior, native parsers/hooks, UI, build, lint, workspace typecheck and the curated Gate. The HTTP fallback has three focused passing UI tests locally and on J, scoped eslint and dashboard typecheck, followed by a successful J production build and live browser acceptance.

Fresh rollback evidence is `/home/ubuntu/fusion-deployments/remote-agents-backup-20260919`. `fusion.dump` is a PostgreSQL custom dump, mode 0600, 100,424,848 bytes, SHA-256 `52c8632dd733145edc6b612a7d34663943a0cbf4aac11578b26f1c55ebfa5302`, and passes `pg_restore --list`. The private daemon config and executable `rollback.py` are retained there. The preceding artifact `/home/ubuntu/fusion-deployments/artifact-02ab7f4f25` remains intact. Do not print or commit credentials.

## Source and ownership

The owned development worktree is `/Users/v/Documents/Codex/2026-09-16/fusion-external-pr1-retry/work/fusion-remote-agents`, branch `codex/remote-agent-feedback-costs`, pushed to `origin`. Read [remote-agent-delivery-plan.md](remote-agent-delivery-plan.md) and [remote-agent-delivery-state.md](remote-agent-delivery-state.md) before work. The exact accepted current-main package and provenance are under `/Users/v/Documents/Codex/2026-09-16/fusion-external-pr1-retry/outputs/fusion-remote-agent-integration/current-main-package-8a83f875-v2`; the self-contained application guide is [remote-agent-current-main-handoff.md](remote-agent-current-main-handoff.md). Fork-only validation workflow/package files are evidence and must not land upstream.

Before editing, inspect all worktrees and peer intent per repository instructions. Work only in a separate Claude worktree and write Claude's intent to that worktree's `.agent-intent/claude.md`. Never use the old J integration worktree with its historical conflicts. Commit by explicit pathspec and keep unrelated peer changes out.

## Remaining work

1. **m5 installation.** The operator confirms m5 is online. `m5` resolves to `172.18.30.5`; J can see port 22, but this Mac's prior direct SSH route returned `No route to host`, and J has no m5 login key. Obtain a directly usable SSH route without copying private keys. Inventory m5 runtime versions, home, existing hooks and service manager; then adapt the m3 standalone installation for host `m5`, preserve all other hooks/settings, create a distinct scoped token, update only the dedicated relay allowlist if its actual source address requires it, and verify connected heartbeat, Codex/Claude visibility and honest costs.
2. **m3 native feedback acceptance.** Visibility and independent GPT-6 Astra accounting already pass on m3. Review normal Codex `/hooks`, reload Claude settings, then use controlled disposable sessions to prove exact-generation feedback delivery and actual agent receipt. Never send an unsolicited message to an active operator agent. Record queued/delivered/uncertain behavior accurately.
3. **Authenticated public acceptance.** The real endpoint is `https://fusion.topkoli.com`; the typed `fusion.topcollie.com` does not resolve. Existing Basic Auth credentials were provided by the operator but must not enter tracked files or logs. Validate authenticated health and the Remote agents UI with a client that supports this flow. Direct WireGuard access is already accepted and should remain the primary route.
4. **Protected source landing.** Production runs a clean local deployment commit, but the implementation is not on protected upstream main. Start from a freshly fetched current main in a new isolated worktree. Apply only the guarded 58-path accepted package, the HTTP fallback commit, and current documentation. Reconcile upstream drift only on owned paths, run required checks, push a review branch and land through repository policy. Do not force-push main, publish npm or cut a release tag.
5. **Closure.** Recheck J/m3/m5 visibility, disposable native feedback, costs, authenticated public access, restart/replay and rollback evidence. Update state with the landed and deployed SHAs and pause the narrow 20-minute heartbeat only when all acceptance items pass.

## Boundaries

Do not add AgentPulse search, notes, pins, launch/stop/resume, templates, full transcript search, patches, summaries/watchers, rankings, notifications or historical migration. Preserve projects/tasks, provider settings, routing, existing auth, AgentPulse services and unrelated systemd/LaunchAgent jobs. Missing telemetry or price data remains unknown rather than zero. Do not derive feedback targets from paths or display titles and do not claim that a delivered hook context proves the model followed the instruction.

Useful first checks on J are read-only: resolve the active package symlink, inspect both user services, call loopback `/api/health` and project-scoped `/api/external-sessions/hosts`, verify the source is clean, and confirm the backup catalog. The state note contains the last accepted evidence and operator testing steps; update it rather than duplicating a new source of truth.
