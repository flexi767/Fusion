---
"@runfusion/fusion": patch
---

summary: Keep PostgreSQL health checks reliable during busy scheduler work.
category: fix
dev: Adds an independent health session and configurable FUSION_PG_POOL_MAX runtime capacity.
