---
"@runfusion/fusion": minor
---

summary: See what a date range of remote agent work cost, broken down by day, model and server.
category: feature
dev: New `GET /external-sessions/overview` aggregating the bounded session scan through the shared pricing seam; reports scan truncation, unpriced and usage-free counts, and the F4-attributed Fusion-run portion as a split of the external total rather than a combined figure.
